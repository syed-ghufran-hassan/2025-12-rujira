# Rujira Account

A Rujira Account is a generic smart contract for which the admin can call any CosmosMsg
through the use of the `sudo` entrypoint. Useful for siloed accounting for things like limit orders in ghost-credit
